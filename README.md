# Stager

## Example
```js
var stager = new Stager();

    stager
        .addTween([greensock tweens])   //append tweens
        .setProgress(val)               //set stage num
        .updateScene();

```